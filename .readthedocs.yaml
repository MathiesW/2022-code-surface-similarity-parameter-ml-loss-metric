version: 2

build:
  os: ubuntu-latest
  tools:
    python: '3.11'

  python:
    install:
      - method: pip
        path: .
      - method: pip
        packages:
          - sphinx
          - furo
          - numpy
          - keras
          - tensorflow

  sphinx:
    configuration: docs/conf.py
